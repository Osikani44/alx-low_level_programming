#include "main.h"

/**
 * more_numbers - prints 10x the numbers
 * Return: Always  0
 */

int  more_numbers(void)
{
	int i, j;

	for (j = 0; j < 15; j++)
	{
	for (i = 0; i = 10; i++)
	{
	if (i >= 10)
	_putchar('1')
	_putchar(i % 10 + '0');
	}
	_putchar('\n')
	}
}
